# Railway service: admin
# Set root directory to monorepo root in the Railway dashboard.
# Dockerfile path: apps/admin/Dockerfile
# Pass NEXT_PUBLIC_* as Docker build args / Railway variables (available at build time).

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/admin/Dockerfile"
watchPatterns = ["apps/admin/**", "packages/shared/**", "package.json", "pnpm-lock.yaml", "pnpm-workspace.yaml"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
